{
  "name": "worldwind-earth-wildfire",
  "version": "2.0.0",
  "private": true,
  "homepage": "https://worldwind.earth/wildfire",
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d root"
  }
}
